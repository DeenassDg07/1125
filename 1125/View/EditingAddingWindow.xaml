<Window x:Class="_1125.View.EditingAddingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1125.View"
        mc:Ignorable="d"
        Title="EditingAddingWindow" Height="450" Width="1000">
    <Border BorderBrush="#2027BC" BorderThickness="15">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="5"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" >
                <Border BorderBrush="#2027BC" BorderThickness="3" Width="370" Height="35" Margin="0,0,15,0">
                    <TextBox Width="360" Text="Поиск:" HorizontalAlignment="Left" Height="33" VerticalAlignment="Center" FontSize="20"  />
                </Border>
                <ComboBox  Width="250" Height="35"  />
            </StackPanel>

            <Button BorderThickness="0" Grid.Row="2" Grid.Column="1" Width="150" HorizontalAlignment="Left" Background="#FF2027BC" Foreground="White" Content="Назад" FontSize="20"/>
            <TextBlock Text="Цена" FontSize="20" Foreground="#2027BC" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Border BorderBrush="#2027BC" BorderThickness="4" Grid.Row="2" Grid.Column="2" Width="200" Height="35" >
                <TextBox/>
            </Border>
            <StackPanel Grid.Row="3" Grid.Column="2">
                
            </StackPanel>

        </Grid>
    </Border>
</Window>