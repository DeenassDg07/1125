<Window x:Class="_1125.View.EntranceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1125.View"
        mc:Ignorable="d"
        Title="EntranceWindow" Height="350" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition />
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding Login}"  FontSize="20" Foreground="#FF555555" Height="40" Grid.Column="1" Grid.Row ="1" Width="200" VerticalAlignment="Bottom" HorizontalAlignment="Center"  />
        <TextBox Text="{Binding Password}" FontSize="20" Foreground="#FF555555" Height="40" Grid.Column="1" Grid.Row ="2" Width="200" />
        <Button BorderThickness="0" Content="Войти" Foreground="White" Background="#FF2027BC" FontSize="20" Grid.Column="1" Grid.Row ="3"  Width="200" Height="40">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border 
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="20">
                        <ContentPresenter 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button  BorderThickness="0"  Click="Button_Click" Foreground="#2027BC" FontSize="20" Grid.Column="1" Grid.Row ="4" Width="200" Height="40" Background="White" VerticalAlignment="Top">
            <TextBlock>
        <Underline>
            Регистрироваться
        </Underline>
            </TextBlock>
        </Button>

    </Grid>
</Window>
