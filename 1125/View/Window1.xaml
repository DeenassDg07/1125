<Window x:Class="_1125.View.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1125.View"
        mc:Ignorable="d"
        Title="Window1" Height="200" Width="700">
    <Border BorderBrush="#FFA4A4A4" BorderThickness="3"  CornerRadius="10">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="125"/>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Image Height="125" Width="125"  Source="/View/Снимок экрана 2025-05-10 195229.png" HorizontalAlignment="Left"/>
        <StackPanel  Grid.Column="1" >

            <StackPanel Orientation="Horizontal">
                <Label Content="Название:" FontSize="13" Foreground="#2027BC"/>
                <TextBlock Width="320" Height="20"/>

            </StackPanel>
            <Border CornerRadius="10" BorderThickness="1" BorderBrush="#FFA4A4A4" Background="#FFA4A4A4" >
                <TextBlock Text="Описание:" FontSize="13" Height="120" />
            </Border>

            <StackPanel Orientation="Horizontal">
                <Label Content="Сколько раз его купили:" FontSize="13" Foreground="#2027BC"/>
                <TextBlock Width="40" Height="17" Background="#FFA4A4A4" Margin="0,0,15,0" />
                <Label Content="Шт:" FontSize="13" Foreground="#2027BC"/>
                <TextBlock Width="40" Height="17" Background="#FFA4A4A4" />
            </StackPanel>

        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" Width="175" >
            <Label Content="Цена:" FontSize="20" Foreground="#2027BC" Height="35"/>
            <Border BorderBrush="#2027BC" BorderThickness="3" Height="35" >
                <TextBlock Width="100" Height="35"/>
            </Border>
        </StackPanel>

        <Button Grid.Column="2" VerticalAlignment="Bottom" FontSize="15" BorderThickness="0" Width="130" Height="35" Foreground="White" Background="#FF2027BC" Content="В корзину" Margin="0,0,0,10">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border 
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
CornerRadius="20">
                        <ContentPresenter 
    HorizontalAlignment="Center" 
    VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

    </Grid>
        </Border>
</Window>
