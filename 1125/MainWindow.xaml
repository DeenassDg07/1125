<Window x:Class="_1125.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1125"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Width" Value="183"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontFamily" Value="Arial Black"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border 
                 x:Name="border"
                 Background="{TemplateBinding Background}"
                 BorderBrush="{TemplateBinding BorderBrush}"
                 BorderThickness="{TemplateBinding BorderThickness}"
                 CornerRadius="16">
                            <ContentPresenter 
      HorizontalAlignment="Center"
      VerticalAlignment="Center"
      TextBlock.FontFamily="{TemplateBinding FontFamily}"
      TextBlock.FontSize="{TemplateBinding FontSize}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFCECECE"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{x:Null}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#FF2027BC">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
            <StackPanel.Background>
                <ImageBrush/>
            </StackPanel.Background>
            <Button Content="Гость" Style="{StaticResource RoundedButton}"  VerticalAlignment="Center" HorizontalAlignment="Center" Height="35" Width="140" />
            <Button Content="Пользователь" Style="{StaticResource RoundedButton}"  VerticalAlignment="Center" HorizontalAlignment="Center" Height="35" Width="140" />
            <Button Content="Директор" Style="{StaticResource RoundedButton}"  VerticalAlignment="Center" HorizontalAlignment="Center" Height="35" Width="140" />
        </StackPanel>

    </Grid>
</Window>
